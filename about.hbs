Biscotti — Third Party Licenses
================================================================================

This file enumerates third-party software components that are redistributed
with Biscotti and the licenses that apply to them. The list is generated by
`cargo about` from the workspace's resolved dependency graph; see `about.toml`
for the configuration.

Overview
--------------------------------------------------------------------------------
{{#each overview}}
- {{{name}}} ({{count}} crate{{#unless (eq count 1)}}s{{/unless}})
{{/each}}

================================================================================

{{#each licenses}}
{{{name}}} ({{id}})
--------------------------------------------------------------------------------

Used by:
{{#each used_by}}
- {{{crate.name}}} {{{crate.version}}}{{#if crate.repository}} <{{{crate.repository}}}>{{/if}}
{{/each}}

License text:

{{{text}}}

================================================================================

{{/each}}
